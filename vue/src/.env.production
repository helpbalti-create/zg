# ─── Prod — Vite reads this when running `vite build` ─────────────────────────
# Replace with your real production domain.
# In production Nginx proxies /oauth/* to Django, so this URL is the public
# domain of the backend (could be the same domain if Django is behind Nginx).

VITE_BACKEND_URL=https://api.zdravy-gorod.md
